// Business Logic
function countUpBy(endNumber, multiplier) {
  if (endNumber.trim() === 0 || multiplier.trim() === 0) {
    return null;
  } else if () {
    
  }

  const finalArray = [];

  for (let i = multiplier; i <= endNumber; i += multiplier) {
    finalArray.push(i);
  }
  return finalArray;
}

// User Interface Logic
